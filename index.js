/**
 * @format
 */

import { AppRegistry } from 'react-native';
import FeaturedProducts from './src/screens/FeaturedProducts';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => FeaturedProducts);
